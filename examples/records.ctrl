type T = {
   x: int,
   y: bool
}

fn record_tester(r: T): int {
   if r.y {
      return r.x;
   } 

   return 0;
}

fn main(): int {
   let t = T { x = 2, y = true };
   return record_tester(t); 
}
